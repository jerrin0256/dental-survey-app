# Copy to .env and set to the same port as your backend (see backend terminal: "Server running on port X").
# Default in code is 5000 if unset. Many setups use PORT=5001 in backend/.env.
EXPO_PUBLIC_DEV_API_PORT=5001
